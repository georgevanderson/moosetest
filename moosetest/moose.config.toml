language = "Typescript"

[redpanda_config]
broker = "localhost:19092"
message_timeout_ms = 1000
retention_ms = 30000

[clickhouse_config]
db_name = "local"
user = "panda"
password = "pandapass"
use_ssl = false
host = "localhost"
host_port = 18123
native_port = 9000

[http_server_config]
host = "localhost"
port = 4000

[logger]
export_to = "http://ga-work-email-moosetest-main-07445d.boreal.cloud/ingest/Logs"

[git_config]
main_branch_name = "main"

[supported_old_versions]
